import react from "react";

import classes from "./ForecastCardsWrapper.module.css";
import ForecastCardsWrapperProps from "./ForecastCardsWrapperProps";

const ForecastCard = (props: ForecastCardsWrapperProps) =>{

    return (
        <p>Card</p>
    );
}

export default ForecastCard;